import { MdBasicColorDirective } from './md-basic-color.directive';

describe('MdBasicColorDirective', () => {
  it('should create an instance', () => {
    const directive = new MdBasicColorDirective();
    expect(directive).toBeTruthy();
  });
});
