import { MdAdvancedColorDirective } from './md-advanced-color.directive';

describe('MdAdvancedColorDirective', () => {
  it('should create an instance', () => {
    const directive = new MdAdvancedColorDirective();
    expect(directive).toBeTruthy();
  });
});
